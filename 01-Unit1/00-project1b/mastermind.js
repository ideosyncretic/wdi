
// generate sequence of 4 randomized pins. factors to randomize are color and index position.
secretPins = []

function generatePins() {
  var pinColor;
  if (math.Random() < 0.3) {
    pinColor = blue
  };
  if ()
  secretPins.push()
}

// correct position
// correct color
console.log(math.Random())
