<%= form_for(jellyfish) do |f| %>
  <% if jellyfish.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(jellyfish.errors.count, "error") %> prohibited this jellyfish from being saved:</h2>

      <ul>
      <% jellyfish.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :scientific_name %>
    <%= f.text_field :scientific_name %>
  </div>

  <div class="field">
    <%= f.label :password_confirmation %>
    <%= f.password_field :password_confirmation %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
